import React from 'react'

export default function Gradient({width,padding,  height, firstColor, secondColor, top, left, bottom, right, className}) {
  return (
    <div className={`${width} ${padding} ${height}, ${firstColor} ${secondColor} ${top} ${left} ${bottom} ${right} ${className} absolute z-1 `}>
      .
    </div>
  )
}
